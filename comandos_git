git ini . -> cria um repo git na pasta atua
git status -> mostra o status do trabalho (de cada arquivo)
git add nome_arq -> add arq para ser commitado
git commit -m "mensagem de lembrete" -> faz o commit do que estava para ser commitado
git log -. mostra todo o hotórico

fLUXO DE TRABALHO

1. criar o diretório do projeto
2. iniciar o git dentro do diretório (git init .)
3. edit ou cria arquivos
4. git status.
5. git add (nome do arquivo) ou add . para todos as diretório da pasta
6. git commit -m "mensagem"
7. volta para o passo 3
8. git log ( para ver o que foi comitado)
9. git diff -> o que mudei no ultimo, estado atual
10. git diff . -> para cada arquivo
11. git reset nome -> volta antes do commit

